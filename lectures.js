// Lectures live under /api/groups/:id/lectures (see groups.js).
// This router is reserved for future direct-lecture endpoints.
const express = require("express");
module.exports = express.Router();
